#file: noinspection YamlXmlServiceInstanceInspection

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        bind:
            # [MERCURE] Инжектируем секрет Mercure во все сервисы, у которых есть
            # аргумент конструктора "string $mercureJwtSecret".
            # Используется в GetChatSubscribeTokenController для подписи JWT-токенов.
            string $mercureJwtSecret: '%env(MERCURE_JWT_SECRET)%'

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    App\State\CollectionEntry\BlackListStateProvider:
        autoconfigure: false
        arguments:
            $collectionProvider: '@api_platform.doctrine.orm.state.collection_provider'
        tags:
            - { name: 'api_platform.state_provider' }

    App\State\CollectionEntry\FavoriteStateProvider:
        autoconfigure: false
        arguments:
            $collectionProvider: '@api_platform.doctrine.orm.state.collection_provider'
        tags:
            - { name: 'api_platform.state_provider' }

    App\State\Localization\Geography\TicketGeographyLocalizationProvider:
        autoconfigure: false
        arguments:
            $itemProvider: '@api_platform.doctrine.orm.state.item_provider'
            $collectionProvider: '@api_platform.doctrine.orm.state.collection_provider'
        tags:
            - { name: 'api_platform.state_provider' }

    App\State\Localization\Geography\UserGeographyLocalizationProvider:
        autoconfigure: false
        arguments:
            $itemProvider: '@api_platform.doctrine.orm.state.item_provider'
            $collectionProvider: '@api_platform.doctrine.orm.state.collection_provider'
        tags:
            - { name: 'api_platform.state_provider' }

    App\State\Localization\Geography\CityLocalizationProvider:
        autoconfigure: false
        arguments:
            $itemProvider: '@api_platform.doctrine.orm.state.item_provider'
            $collectionProvider: '@api_platform.doctrine.orm.state.collection_provider'
        tags:
            - { name: 'api_platform.state_provider' }

    App\State\Localization\Geography\DistrictLocalizationProvider:
        autoconfigure: false
        arguments:
            $itemProvider: '@api_platform.doctrine.orm.state.item_provider'
            $collectionProvider: '@api_platform.doctrine.orm.state.collection_provider'
        tags:
            - { name: 'api_platform.state_provider' }

    App\State\Localization\Geography\ProvinceLocalizationProvider:
        autoconfigure: false
        arguments:
            $itemProvider: '@api_platform.doctrine.orm.state.item_provider'
            $collectionProvider: '@api_platform.doctrine.orm.state.collection_provider'
        tags:
            - { name: 'api_platform.state_provider' }

    App\State\Localization\Title\CategoryTitleLocalizationProvider:
        autoconfigure: false
        arguments:
            $itemProvider: '@api_platform.doctrine.orm.state.item_provider'
            $collectionProvider: '@api_platform.doctrine.orm.state.collection_provider'
        tags:
            - { name: 'api_platform.state_provider' }

    App\State\Localization\Title\OccupationTitleLocalizationProvider:
        autoconfigure: false
        arguments:
            $itemProvider: '@api_platform.doctrine.orm.state.item_provider'
            $collectionProvider: '@api_platform.doctrine.orm.state.collection_provider'
        tags:
            - { name: 'api_platform.state_provider' }

    App\State\Localization\Title\UnitTitleLocalizationProvider:
        autoconfigure: false
        arguments:
            $itemProvider: '@api_platform.doctrine.orm.state.item_provider'
            $collectionProvider: '@api_platform.doctrine.orm.state.collection_provider'
        tags:
            - { name: 'api_platform.state_provider' }

    App\State\Localization\Title\AppealReasonLocalizationProvider:
        autoconfigure: false
        arguments:
            $itemProvider: '@api_platform.doctrine.orm.state.item_provider'
            $collectionProvider: '@api_platform.doctrine.orm.state.collection_provider'
        tags:
            - { name: 'api_platform.state_provider' }

    App\State\Localization\Title\LegalLocalizationProvider:
        autoconfigure: false
        arguments:
            $itemProvider: '@api_platform.doctrine.orm.state.item_provider'
            $collectionProvider: '@api_platform.doctrine.orm.state.collection_provider'
        tags:
            - { name: 'api_platform.state_provider' }

    App\Serializer\UserContextBuilder:
        decorates: 'api_platform.serializer.context_builder'
        arguments:
            # noinspection MissingServiceYaml
            $decorated: '@.inner'
            $security: '@security.helper'
